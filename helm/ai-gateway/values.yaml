# optimus-ide-collab -- Common workload configuration options.
optimus-ide-collab:
  # optimus-ide-collab.env -- Additional environment variables for the AI Gateway
  # container. These are rendered after chart-configured variables, including
  # when a variable name is duplicated.
  env: []
  # - name: "OPTIMUS-IDE-COLLAB_VERBOSE"
  #   value: "true"

  # optimus-ide-collab.envFrom -- Additional envFrom sources for the AI Gateway container.
  envFrom: []

  # optimus-ide-collab.image -- The image to use for the AI Gateway.
  image:
    # optimus-ide-collab.image.repo -- The repository of the image.
    repo: "ghcr.io/optimus-ide-collab/optimus-ide-collab"
    # optimus-ide-collab.image.tag -- The tag of the image, defaults to {{.Chart.AppVersion}}
    # if not set. If you're using the chart directly from git, the default
    # app version will not work and you'll need to set this value. The helm
    # chart helpfully fails quickly in this case.
    tag: ""
    # optimus-ide-collab.image.pullPolicy -- The pull policy to use for the image. See:
    # https://kubernetes.io/docs/concepts/containers/images/#image-pull-policy
    pullPolicy: IfNotPresent
    # optimus-ide-collab.image.pullSecrets -- The secrets used for pulling the Optimus-IDE-Collab image
    # from a private registry.
    pullSecrets: []
    #  - name: "pull-secret"

  # optimus-ide-collab.initContainers -- Init containers for the deployment. See:
  # https://kubernetes.io/docs/concepts/workloads/pods/init-containers/
  initContainers:
    []
    # - name: init-container
    #   image: busybox:1.28
    #   command: ['sh', '-c', "sleep 2"]

  # optimus-ide-collab.annotations -- The Deployment annotations. See:
  # https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
  annotations: {}

  # optimus-ide-collab.labels -- The Deployment labels. See:
  # https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
  labels: {}

  # optimus-ide-collab.podAnnotations -- The pod annotations. See:
  # https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
  # The annotation `app.kubernetes.io/component: ai-gateway` is added
  # automatically.
  podAnnotations: {}

  # optimus-ide-collab.podLabels -- The pod labels. See:
  # https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
  podLabels: {}

  # optimus-ide-collab.serviceAccount -- Configuration for the automatically created
  # service account.
  serviceAccount:
    # optimus-ide-collab.serviceAccount.annotations -- The service account annotations.
    annotations: {}
    # optimus-ide-collab.serviceAccount.name -- The service account name.
    name: optimus-ide-collab-ai-gateway
    # optimus-ide-collab.serviceAccount.disableCreate -- Whether to create the service
    # account or use an existing service account.
    disableCreate: false

  # optimus-ide-collab.securityContext -- Fields related to the container's security
  # context (as opposed to the pod). Some fields are also present in the pod
  # security context, in which case these values will take precedence.
  securityContext:
    # optimus-ide-collab.securityContext.runAsNonRoot -- Requires that the container
    # runs as an unprivileged user.
    runAsNonRoot: true
    # optimus-ide-collab.securityContext.runAsUser -- Sets the user id of the container.
    runAsUser: 1000
    # optimus-ide-collab.securityContext.runAsGroup -- Sets the group id of the container.
    runAsGroup: 1000
    # optimus-ide-collab.securityContext.seccompProfile -- Sets the seccomp profile for
    # the container.
    seccompProfile:
      type: RuntimeDefault
    # optimus-ide-collab.securityContext.allowPrivilegeEscalation -- Controls whether
    # the container can gain additional privileges.
    allowPrivilegeEscalation: false

  # optimus-ide-collab.podSecurityContext -- Pod-level security context settings. See:
  # https://kubernetes.io/docs/tasks/configure-pod-container/security-context/
  podSecurityContext: {}

  # optimus-ide-collab.volumes -- A list of extra volumes to add to the AI Gateway pod.
  volumes: []
  # - name: "my-volume"
  #   emptyDir: {}

  # optimus-ide-collab.volumeMounts -- A list of extra volume mounts to add to the AI
  # Gateway container.
  volumeMounts: []
  # - name: "my-volume"
  #   mountPath: "/mnt/my-volume"

  # optimus-ide-collab.replicaCount -- The number of AI Gateway pods.
  replicaCount: 1

  # optimus-ide-collab.lifecycle -- Container lifecycle handlers, allowing for lifecycle
  # events such as postStart and preStop. See:
  # https://kubernetes.io/docs/tasks/configure-pod-container/attach-handler-lifecycle-event/
  lifecycle:
    {}
    # postStart:
    #   exec:
    #     command: ["/bin/sh", "-c", "echo postStart"]

  # optimus-ide-collab.resources -- The resources to request for the AI Gateway container.
  resources:
    requests:
      cpu: "1"
      memory: 1Gi

  # optimus-ide-collab.startupProbe -- Startup probe configuration for the AI Gateway.
  startupProbe:
    enabled: false
    initialDelaySeconds: 0

  # optimus-ide-collab.livenessProbe -- Liveness probe configuration for the AI Gateway.
  # This checks HTTP server responsiveness. Readiness reflects the established
  # connection to optimus-ide-collabd and initial provider configuration loaded.
  livenessProbe:
    enabled: true
    initialDelaySeconds: 0

  # optimus-ide-collab.readinessProbe -- Readiness probe configuration for the AI Gateway.
  readinessProbe:
    enabled: true
    initialDelaySeconds: 0

  # optimus-ide-collab.certs -- CA bundles to mount inside the AI Gateway pod.
  certs:
    # optimus-ide-collab.certs.secrets -- A list of CA bundle secrets to mount into the
    # pod. The secrets should exist in the same namespace as the Helm
    # deployment. The given key in each secret is mounted at
    # `/etc/ssl/certs/{secret_name}.crt`.
    secrets:
      []
      # - name: "my-ca-bundle"
      #   key: "ca-bundle.crt"

  # optimus-ide-collab.affinity -- Allows specifying an affinity rule for the deployment.
  affinity: {}

  # optimus-ide-collab.tolerations -- Tolerations for tainted nodes. See:
  # https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
  tolerations: []

  # optimus-ide-collab.nodeSelector -- Node labels for constraining pods to nodes. See:
  # https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
  nodeSelector: {}

  # optimus-ide-collab.topologySpreadConstraints -- Topology spread constraints for the
  # pods. See:
  # https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/
  topologySpreadConstraints: []

  # optimus-ide-collab.priorityClassName -- The priority class name for the pods.
  priorityClassName: ""

  # optimus-ide-collab.command -- The command to use when running the container. Used
  # for customizing the location of the `optimus-ide-collab` binary in your image.
  command:
    - /opt/optimus-ide-collab

# Authentication and listener settings for the standalone gateway.
aigateway:
  # Optional existing Secret containing an AI Gateway key. When unset, provide
  # OPTIMUS-IDE-COLLAB_AI_GATEWAY_KEY or OPTIMUS-IDE-COLLAB_AI_GATEWAY_KEY_FILE through optimus-ide-collab.env.
  keySecret:
    name: ""
    key: key
  optimus-ide-collabTLS:
    caSecret:
      name: ""
      key: ca.crt
    clientSecret:
      name: ""
      certKey: tls.crt
      keyKey: tls.key
  # The certificate must cover every hostname clients use.
  listenerTLS:
    name: ""
    certKey: tls.crt
    keyKey: tls.key
  # Allow 5 seconds for provider reload shutdown, 300 seconds for HTTP
  # draining, 5 seconds for daemon shutdown, 5 seconds for tracing shutdown,
  # and 15 seconds of termination headroom.
  terminationGracePeriodSeconds: 330

# Stable data-plane Service fronting the container's port 4001 listener.
service:
  enable: true
  type: ClusterIP
  port: 80
  annotations: {}
  sessionAffinity: None
  externalTrafficPolicy: Cluster
  loadBalancerClass: ""
  loadBalancerIP: ""
  nodePort: null

# Optional networking.k8s.io/v1 Ingress.
ingress:
  enable: false
  className: ""
  annotations: {}
  host: ""
  path: /
  pathType: Prefix
  tls:
    enable: false
    secretName: ""

# Optional Gateway API HTTPRoute. Requires the v1 HTTPRoute CRD.
httproute:
  enable: false
  parentRefs: []
  hostnames: []
  annotations: {}

# Additional templated Kubernetes manifests.
extraTemplates: []
